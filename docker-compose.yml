version: '3.8'
services:
  web:
    build: .
    image: line-gemini-app:latest
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
